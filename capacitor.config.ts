import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'accordion-modal',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
